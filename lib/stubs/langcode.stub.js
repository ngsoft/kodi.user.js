/**
 * Find a lang
 * @param {string} lang 
 * @returns {Promise} 
 */
function langcode(lang) {
    return new Promise();
}

langcode.database = [];

langcode.map = new Map();
langcode.map2 = new Map();
langcode.map3 = new Map();
langcode.mapTags = new Map();

const root = { langcode };